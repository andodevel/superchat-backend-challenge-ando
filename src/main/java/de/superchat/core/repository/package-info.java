package de.superchat.core.repository;
/**
 * This module package is to serve as core platform microservice to configuration the system and
 * provide some core feature like initialize chat between 2 parties(any of type user, channel, group).
 * Unfortunately, at this point, this module is not fully implemented yet and not be in use.
 *
 * DB access: configuration table like 'source', 'chat_type', and platform, collaboration table like 'room'.
 *
 * Main functionalities:
 * 1. Configure the application
 * 2. Orchestration, provide core function and feature for others.
 *
 */