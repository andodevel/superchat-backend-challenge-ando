package de.superchat.message;
/**
 * This module package is to serve as message microservice.
 *
 * DB access: it owns 'message' table and does not share with others.
 *
 * Main functionalities:
 * 1. Send message to other internal Superchat users.
 * 2. Create new user. That could be serve as register function.
 *
 */